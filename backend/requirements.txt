# Django MongoDB Backend (same DB as Node backend)
# Use Django 5.x for broader compatibility (Django 6 may be unavailable in some build envs)
Django>=5.0,<6
django-mongodb-backend>=5.0,<6
django-cors-headers>=4.0

# Auth
PyJWT[crypto]>=2.8
bcrypt>=4.0

# HTTP & env
requests>=2.28
python-dotenv>=1.0

# Pinyin for OCR response
pypinyin>=0.50

# Production WSGI (used in Docker)
gunicorn>=21

# Optional: ChatTTS for TTS. Uncomment to enable AI voice (otherwise browser TTS fallback).
# pip install ChatTTS torch torchaudio numpy transformers==4.53.2
# ChatTTS>=0.2
# transformers==4.53.2  # 4.53.2 可避免 narrow() 报错，见 ChatTTS#955
